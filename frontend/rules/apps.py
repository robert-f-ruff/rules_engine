"""Define the rules application."""
from django.apps import AppConfig


class RulesConfig(AppConfig):
    """The rules application definition."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'rules'
